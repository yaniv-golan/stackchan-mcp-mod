# Copy to .env (which is gitignored) and fill in.
# Used by the preference script when setting robot preferences with @env: references;
# the robot stores these values in plaintext flash, so prefer restricted, low-budget keys.

# API key for the TTS provider configured on the robot (tts.type=openai).
OPENAI_API_KEY=
